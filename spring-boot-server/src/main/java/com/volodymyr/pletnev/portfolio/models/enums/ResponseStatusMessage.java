package com.volodymyr.pletnev.portfolio.models.enums;

public enum ResponseStatusMessage {
	USER_NOT_FOUND("User not found"),
	ORDER_NOT_FOUND("Order not found");

	ResponseStatusMessage(String s) {
	}
}
